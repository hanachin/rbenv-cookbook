export RBENV_ROOT=<%= @rbenv_root %>
export PATH=$RBENV_ROOT/bin:<%= @ruby_build_bin_path %>:$PATH

shell="$(basename "$SHELL")"
case "$shell" in
bash | zsh )
  source $RBENV_ROOT/completions/rbenv.${shell}
esac

eval "$(rbenv init -)"
